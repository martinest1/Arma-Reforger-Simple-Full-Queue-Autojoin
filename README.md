Windows only

Requirements: python
Required dependencies (run in cmd): pip install keyboard pillow

If you have bad ping/bad pc, you may need to increase the time.sleep(1.3) at LINE 54 from 1.3 sec to 1.5 or 2.

How to use:
Hover over server join arrow in browser, press F6, when in queue close script
